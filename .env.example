POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

POSTGRES_HOST_PORT=
APP_HOST_PORT=


DATABASE_URL=

AUTH_SECRET=
# Public origin of the app (e.g. https://example.com). Optional in local dev;
# set it in production so NextAuth builds absolute URLs from the real domain
# instead of the container bind address (0.0.0.0).
AUTH_URL=

SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=

AWS_REGION=
AWS_S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_PUBLIC_BASE_URL=
